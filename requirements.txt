# Versión estable más reciente (abril 2026). Requiere Python 3.12+.
Django>=6.0,<6.1

# Manejo de imágenes para ImageField.
Pillow>=10.4

# Sirve estáticos comprimidos en producción sin necesidad de Nginx.
whitenoise>=6.6

# WSGI para producción.
gunicorn>=22.0

# Carga variables del archivo .env automáticamente.
python-dotenv>=1.0

# OPCIONAL: descomenta si quieres cambiar a PostgreSQL.
# psycopg[binary]>=3.2

#sms
twilio